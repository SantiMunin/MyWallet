<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.udc.santiago"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="7" />
    <!-- Para poder ejecutar test JUnit con Android -->
    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:label="Unit tests"
        android:targetPackage="es.udc.santiago" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <uses-library android:name="android.test.runner" />

        <activity android:name=".view.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.categories.ManageCategoriesActivity" />
        <activity android:name=".view.categories.EditCategoryActivity" />
        <activity android:name=".view.cashflows.AddOperationActivity" />
        <activity android:name=".view.OptionsActivity" />
        <activity android:name=".view.cashflows.OverviewActivity" />
    </application>

</manifest>